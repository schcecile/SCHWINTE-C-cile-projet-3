{% extends 'base.html.twig' %}

{% block title %}Modifier l'événement{% endblock %}

{% block body %}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('main') }}">Calendrier</a></li>
            <li class="breadcrumb-item"><a href="{{ path('calendar_index') }}">Liste des événements</a></li>
            <li class="breadcrumb-item active" aria-current="page">Modifier l'événement</li>
        </ol>
    </nav>
    <div class="container py-5">
        <h1>Modifier l'événement</h1>

        {{ include('calendar/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        {{ include('calendar/_delete_form.html.twig') }}
    </div>
{% endblock %}
