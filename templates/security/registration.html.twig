{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="border rounded p-5">
            <h1 class="text-center pb-4" >Inscription sur le site</h1>

            {{ form_start(form) }}
                {{ form_row(form.user_name, {'attr': {'placeholder': 'Nom d\'utilisateur...'}}) }}
                {{ form_row(form.first_name, {'attr': {'placeholder': 'Prénom...'}}) }}
                {{ form_row(form.last_name, {'attr': {'placeholder': 'Nom...'}}) }}
                {{ form_row(form.email, {'attr': {'placeholder': 'Adresse email...'}}) }}
                {{ form_row(form.password, {'attr': {'placeholder': 'Mot de passe...'}}) }}
                {{ form_row(form.confirm_password, {'attr': {'placeholder': 'Répétez votre mot de passe...'}}) }}
                {{ form_row(form.agree_terms) }}
                <div class="text-center">
                    <button type="submit" class="btn btn-success ">Inscription !</button>
                </div>
            {{ form_end(form) }}
    </div>
</div>

{% endblock %}
